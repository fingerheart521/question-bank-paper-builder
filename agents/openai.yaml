interface:
  display_name: "Question Bank Paper Builder"
  short_description: "Build audited randomized exam papers"
  default_prompt: "Use $question-bank-paper-builder (v1.0) to organize these question-bank files and generate audited randomized papers."
policy:
  allow_implicit_invocation: true
